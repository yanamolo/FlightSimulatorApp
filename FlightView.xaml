<Page x:Class="FlightSimulatorApp.FlightView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:Controls="clr-namespace:FlightSimulatorApp.Controls"
      xmlns:local="clr-namespace:FlightSimulator"
      mc:Ignorable="d" 
      d:DesignHeight="1000" d:DesignWidth="1500" Title="Flight-Gear">

    <Grid>
        <Canvas>
            <Viewbox Width="400" Height="400" Canvas.Left="162" Canvas.Top="89">
                <Controls:controlsDisplay x:Name="controlsDisplay"/>
            </Viewbox>
            <Viewbox Width="600" VerticalAlignment="Center" Canvas.Left="779" Canvas.Top="89">
                <Controls:JoystickSliders x:Name="JoystickSliders"  Margin="0,50,50,0" VerticalAlignment="Center" Width="626"/>
            </Viewbox>
            <Viewbox Width="800" Margin="100 0 0 50">
                <Controls:Bing_Map x:Name="Bing_Map" Margin="44,168,-44.4,102.2"/>
            </Viewbox>
        </Canvas>
    </Grid>
</Page>
